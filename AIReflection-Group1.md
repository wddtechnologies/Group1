*~*~*~*~*~*~*~*~*~*~*~*
!    AI Reflection    !
*~*~*~*~*~*~*~*~*~*~*~*

What AI was used for:

- Debugging merge conflicts across 6 files that were blocking the project from compiling
- Implementing the JewelryService with all CRUD methods since the original was empty
- Adding form validation and error handling patterns

What we did ourselves:

- Designed the original project structure and component layout
- Created the jewelry data model and database seed data
- Built the initial component templates and navigation
- Selected and added product images
- Managed the GitHub repository and coordinated group work
- Tested the application and identified bugs
- Implemented ChangeDetectorRef to manage the Angular loading issues

What we learned from the AI interaction:
- How ChangeDetectorRef works and when Angular needs manual change detection triggers
- The full lifecycle of an HTTP request in Angular: service → Observable → subscribe → UI update
- How template-driven form validation works with #nameCtrl="ngModel" and error display
- Why merge conflicts happen and how to prevent them by splitting work across different files
- How json-server acts as a persistent database by writing directly to db.json
- Git workflow for group projects: always pull before push



